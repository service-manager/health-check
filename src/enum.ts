export enum Status {
    Up = "up",
    Down = "down"
}

export enum Event {
    Up = "up",
    Down = "down",
    Status = "status",
    Start = "start",
    Stop = "stop",
    Timeout = "timeout"
}
