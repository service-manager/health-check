export enum HealthCheckStatus {
    Up = "up",
    Down = "down"
}

export enum HealthCheckEvent {
    Up = "up",
    Down = "down",
    Change = "change",
    Start = "start",
    Stop = "stop",
    Timeout = "timeout"
}
